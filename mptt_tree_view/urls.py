# from django.conf.urls import patterns
# from djangoautoconf.model_utils.url_for_models import add_all_urls
#
# from obj_sys import models
#
# urlpatterns = patterns('',
#                        )
#
#
# add_all_urls(urlpatterns, models)

# t = TreeTastypieApiGenerator(models.UfsObj)
# v1_api = NamespacedApi(api_name='v1', urlconf_namespace="mptt_tree_view")
#
# urlpatterns += t.get_url_list(v1_api)
