============
Installation
============

At the command line::

    $ easy_install mptt-tree-view

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mptt-tree-view
    $ pip install mptt-tree-view
